version: '3.8'

services:
  web:
    build: .
    container_name: inventarios_web
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=production
    networks:
      - coolify
    restart: unless-stopped

networks:
  coolify:
    external: true
